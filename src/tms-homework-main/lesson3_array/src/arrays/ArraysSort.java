package tms;

import java.util.Arrays;

public class ArraysSort {
    static void main() {
        int[] array = {1, 2, 65, 23, 54, 1};
        Arrays.sort(array);
        System.out.println(Arrays.toString(array));
    }
}
